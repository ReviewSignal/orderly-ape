---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.1-0.20250829124250-5d6015328208
  name: testscenarios.ape.reviewsignal.com
spec:
  group: ape.reviewsignal.com
  names:
    kind: TestScenario
    listKind: TestScenarioList
    plural: testscenarios
    singular: testscenario
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: TestScenario is the Schema for the testscenarios API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TestScenario
            properties:
              envVars:
                description: EnvVars is a list of environment variables to set for
                  the test run.
                items:
                  properties:
                    name:
                      description: |-
                        Name of the environment variable. Must be a C_IDENTIFIER.
                        It cannot be TARGET, or start with K6_ or APE_ as these are reserved for the test scenario and system.
                      minLength: 1
                      type: string
                      x-kubernetes-validations:
                      - message: Name cannot be TARGET or start with K6_ or APE_ as
                          these are reserved.
                        rule: '!matches(self.upperAscii(), ''^(TARGET|K6_.*|APE_.*)$'')'
                    value:
                      description: Value of the environment variable.
                      type: string
                  required:
                  - name
                  type: object
                maxItems: 24
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              git:
                properties:
                  path:
                    default: loadtest.js
                    description: Path is the path within the repository to the test
                      scenario script
                    minLength: 1
                    type: string
                  repository:
                    description: |-
                      Repository is the URL of the git repository containing the test scenario
                      For example:
                      - https://github.com/ReviewSignal/loadtesting

                      We currently only support public repositories via http.
                    minLength: 10
                    pattern: ^(https?|git)://[^\s/$.?#].[^\s]*$
                    type: string
                  revision:
                    default: main
                    description: Revision is the git revision (branch, tag, commit)
                      to checkout
                    minLength: 1
                    pattern: ^[A-Za-z0-9._/-]+$
                    type: string
                required:
                - path
                - repository
                - revision
                type: object
              labels:
                additionalProperties:
                  type: string
                description: Labels are test results labels to set for the test run.
                maxProperties: 24
                type: object
              maxDuration:
                default: 30m
                description: |-
                  MaxDuration is the maximum duration the test scenario is allowed to run.
                  This is a safety measure to prevent runaway tests.
                  It must be a valid duration string. For example: "30m", "1h", "2h45m".
                  If not specified, it defaults to "30m".
                  It must account for the time taken to set up the test scenario (dowloading docker image, worker synchronization
                  time, etc.).
                type: string
              resources:
                description: Resources defines the resource requests and limits for
                  the test scenario execution.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              script:
                description: Script is the URL of the test scenario script
                type: string
              workers:
                description: Workers is a list of references to Workers (locations)
                  where the test should run.
                items:
                  description: TestRunWorkerSpec defines the specification of a worker
                    in a TestRun.
                  properties:
                    name:
                      description: name is unique within a namespace to reference
                        an object resource.
                      minLength: 1
                      type: string
                    namespace:
                      description: namespace defines the space within which the object
                        name must be unique.
                      type: string
                    numWorkers:
                      default: 1
                      description: NumWorkers is the number of workers to use for
                        this location.
                      format: int32
                      minimum: 1
                      type: integer
                  required:
                  - name
                  - namespace
                  type: object
                  x-kubernetes-map-type: atomic
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            type: object
            x-kubernetes-validations:
            - message: Only one of 'git' or 'script' must be specified
              rule: '((has(self.git) ? 1 : 0) + (has(self.script) ? 1 : 0)) < 2'
          status:
            description: status defines the observed state of TestScenario
            properties:
              conditions:
                description: The status of each condition is one of True, False, or
                  Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
