apiVersion: v1
kind: Service
metadata:
  name: controller-manager-http
spec:
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: orderly-ape
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
